{
  "id": "${plugin.id}",
  "name": "${plugin.prefix}",
  "version": "${plugin.version}",
  "description": "${plugin.description}",
  "url": "${plugin.website}",
  "main": "${plugin.main}",
  "authors": ["SirBlobman"],
  "dependencies": []
}
